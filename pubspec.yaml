name: time_dropper
description: Gorgeous, fully animated, round dial time picker inspired by TimeDropperJS
version: 0.0.1
homepage: https://github.com/rajyadavnp/time_dropper

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
